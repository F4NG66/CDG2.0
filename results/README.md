# Results

| Path | Status |
|---|---|
| [`generalized_safety/`](generalized_safety/) | **Final program results** (M1, M2, M3, M3′, plus lineage). Built from SHA-verified frozen artifacts; start with `INDEX.json`. |
| [`frozen_35_summary.json`](frozen_35_summary.json) | **Historical.** An earlier frozen 35-pair steering replication from the RRAE-era pipeline (config: [`../configs/final_rrae_steering.yaml`](../configs/final_rrae_steering.yaml)). Its contents are unchanged and frozen, including its `conclusion` field. That field records the conclusion of that replication only, **not** the final project conclusion, which is in [`../docs/EXPERIMENTAL_HISTORY.md`](../docs/EXPERIMENTAL_HISTORY.md). |
