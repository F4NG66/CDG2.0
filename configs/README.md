# Configs

| Path | Status |
|---|---|
| [`generalized_safety/final_program.yaml`](generalized_safety/final_program.yaml) | **Final program index** (M1 → M3′): stage status, pointers, and frozen settings copied from the result summaries. |
| [`final_rrae_steering.yaml`](final_rrae_steering.yaml) | **Historical.** Frozen RRAE selection and the frozen 35-pair steering-replication specification. Its settings, including the `paper_path: final_large_scale_frozen_replication` label, are left unchanged for reproducibility. That label predates the generalized safety-direction program and does not mark the final project result. |
